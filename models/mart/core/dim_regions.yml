---
version: 2
models:
  - name: dim_regions
    description: Regions dimensions table
    config:
      group: marketing
    columns:
      - name: region_key
        description: primary key of the model
        data_tests:
          - unique
          - not_null
      - name: name
        description: region name
      - name: comment
        description: '{{ doc("comment") }}'
