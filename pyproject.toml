[project]
name = "snowflake-dbt-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "dbt-core~=1.9.0",
    "dbt-snowflake~=1.9.0",
]
